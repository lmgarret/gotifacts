# Binaries
/gotifacts
*.exe

# Local data / SQLite
/data/
*.db
*.db-wal
*.db-shm

# Node
web/node_modules/
docs/node_modules/

# Astro / Starlight docs site (built output + cache)
docs/dist/
docs/.astro/

# Built SPA (regenerated by `npm run build`; only .gitkeep is tracked)
web/dist/*
!web/dist/.gitkeep

# Env
.env
.env.local

# Editor / OS
.DS_Store
*.swp
